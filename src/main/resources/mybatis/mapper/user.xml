<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skkcandle.dao.UserDao">
	<!-- userEmail로 유저 정보 가져오기 -->
	<select id="select" parameterType="String" resultType="User">
		select *from userinfo where userEmail = #{userEmail}
	</select>
	
	<!-- userId로 유저 정보 가져오기 -->
	<select id="selectById" parameterType="int" resultType="User">
		select *from userinfo where userId = #{userId}
	</select>
	
	<!-- 회원가입하기 -->
	<insert id="insert"  parameterType="User">
		<selectKey keyProperty="userId" resultType="int" order="BEFORE">
			select user_seq.nextval from dual <!-- 시퀀스의 다음 번호를 가지고온다. 그리고 다음단계=>세팅하기 -->
		</selectKey>
		insert into userinfo (userId, userName, userPassword, userEmail, userPhone, userPostcode, userRoadAddress, userDetailAddress, userJoinDate, userBirthDay, userGrade ,userPoint)
		values (#{userId}, #{userName}, #{userPassword}, #{userEmail}, #{userPhone}, #{userPostcode},#{userRoadAddress},#{userDetailAddress}, sysdate, sysdate, '평민' ,0) 
	</insert>
	
	<!-- 회원 이미지 업로드 하기  -->
	<update id="updateImage" parameterType="User">
		update userinfo set
	 		userAttachType = #{userAttachType, jdbcType=VARCHAR},
		 	userImageData = #{userImageData, jdbcType=BLOB}
			where userId=#{userId}
	</update>
	<!-- 회원 정보 업로드 하기  -->
	<update id="updateUserInfo" parameterType="User">
		update userinfo set
	 		userName = #{userName},	userPassword = #{userPassword},
	 		userPhone=#{userPhone}, userPostcode=#{userPostcode},
	 		userRoadAddress = #{userRoadAddress}, userDetailAddress=#{userDetailAddress}
			where userId=#{userId}
	</update>
	<!-- 비밀번호 제외 회원정보 업로드 -->
	<update id="updateUserInfoNoPW" parameterType="User">
		update userinfo set
	 		userName = #{userName},
	 		userPhone=#{userPhone}, userPostcode=#{userPostcode},
	 		userRoadAddress = #{userRoadAddress}, userDetailAddress=#{userDetailAddress}
			where userId=#{userId}
	</update>
	
	<!-- 회원탈퇴 -->
	<delete id="delete" parameterType="int">
		delete from userinfo where userId=#{userId}
	</delete>
	
	<!-- 회원 포인트 적립 혹은 사용 -->
	<update id="changeUserPoint" parameterType="User">
		update userinfo set
			userPoint = #{userPoint}
			where userId = #{userId}
	</update>
	
</mapper>